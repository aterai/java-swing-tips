# Configuration cache is disabled on purpose: it silently disables
# configuration-on-demand, and serializing 1200+ project models is slower than
# the configuration it saves on this repository.
org.gradle.configuration-cache=false
org.gradle.configureondemand=true
org.gradle.caching=true
org.gradle.parallel=false
org.gradle.jvmargs=-Xmx1g
